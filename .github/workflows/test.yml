name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Prepare
        uses: ./.github/actions/prepare

      - name: Run tests
        run: pnpm test
