name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Prepare
        uses: ./.github/actions/prepare

      - name: Run builds
        run: pnpm build
